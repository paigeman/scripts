apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
   name: grafana
   namespace: kube-system
spec:
   rules:
   - host: learn.yzj.icu
     http:
       paths:
       - path: /
         backend:
           service:
             name: grafana
             port:
               number: 3000
